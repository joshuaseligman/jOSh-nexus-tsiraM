module TSOS {
    /**
     * Enum to represent the current status of the CPU execute state
     */
    export enum ExecuteState {
        EXECUTE0,
        EXECUTE1,
        EXECUTE2,
        EXECUTE3,
        EXECUTE4,
        EXECUTE5,
        EXECUTE6,
        EXECUTE7
    }
}